# manim-web 更新日志

> 版本变更记录

---

## v2.0.15 — 2026-08-09

### 新增

- **WORK_DIR 5 级自动检测**：pip 全局安装后零配置自动定位工作目录
  1. CLI `--work-dir` 参数
  2. `MANIM_WEB_WORK_DIR` 环境变量
  3. 从 cwd 向上查找 `.joycode/mcp.json`（项目根标记）
  4. IDE workspace storage 搜索（JoyCode/VSCode/Cursor/Windsurf，Win/Linux/Mac）
  5. 回退到 cwd
- **MCP roots 懒校正**：首次工具调用时通过 `ctx.session.list_roots()` 获取客户端工作区，作为第 6 级安全网
- **`_update_work_dir()` 级联更新**：校正 WORK_DIR 时自动同步 PROJECTS_DIR、_DOCS_TARGET、deploy_docs()
- **Context 导入安全保护**：`from mcp.server.mcpserver.context import Context` 用 try/except 包裹，确保任何 MCP SDK 版本不崩溃

### 修复

- **版本号同步**：`__init__.__version__` 与 `pyproject.toml` 版本号统一
- **MCP roots 守卫**：`_try_correct_work_dir_from_roots` 添加 `_MCPContext is None` 检查

---

## v2.0.1 — 2026-08-08

### 修复

- **静态图形导出缺少 self.add()**：`add_mobject` 现在同时记录 `c = Circle(...)` 和 `self.add(c)`，导出代码可直接运行
- **relaxed 沙箱安全加固**：从白名单移除 `os` 模块（允许 `os.path`），防止通过 `os.system()` 等执行危险操作

---

## v2.0 — 2026-08-07

### 架构重构

- **单进程架构**：移除 TCP 服务器层，`DirectManimSession` 直接操作 manim Scene
- **看门狗机制**：`_ensure_healthy` 自动检测并重建异常会话（最多 3 次）
- **延迟恢复**：`_pending_restore` 等待 WebSocket 连接后再播放恢复动画

### 新增

- 16 个 MCP 工具完整覆盖：会话管理、图形添加、动画播放、代码导出、视频渲染
- 跨对话持久化：`state.json` 保存场景代码，重启后自动恢复
- 三级沙箱：strict / relaxed / full
- WebSocket 二进制帧推送：PNG 帧直接传输，浏览器实时预览
- 守护进程模式：`daemon=true` 后台运行，开机自启动支持

### 工具列表

| 类别 | 工具 |
|------|------|
| 会话 | web_persistent_start, web_persistent_stop, web_persistent_status |
| 图形 | add_mobject, remove_mobject |
| 动画 | play, play_with_targets |
| 导出 | export_code, render_video |
| 查询 | get_mobject_info, get_animation_types, get_available_mobjects |
| 辅助 | reset_scene, get_log |

---

## v1.x — 2026-07

### 初始版本

- TCP 服务器 + WebSocket 代理架构
- 基础 MCP 工具集
- 浏览器预览功能
- 代码导出功能